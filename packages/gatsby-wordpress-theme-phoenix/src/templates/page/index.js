import React from "react";
import Layout from "../../components/layout";

const Page = ( props ) => {

	console.warn( 'props', props );

	return (
		<Layout>
			Hello
		</Layout>
	)
};
export default Page;
